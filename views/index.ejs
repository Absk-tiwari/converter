<!doctype html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="shortcut icon" href="img/favicon.ico" type="image/x-icon">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="assets/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="assets/slick/slick.css">
    <link rel="stylesheet" href="assets/slick/slick-theme.css">
    <!-- icon css-->
    <link rel="stylesheet" href="assets/elagent-icon/style.css">
    <link rel="stylesheet" href="assets/niceselectpicker/nice-select.css">
    <link rel="stylesheet" href="assets/animation/animate.css">
    <link rel="stylesheet" href="assets/mcustomscrollbar/jquery.mCustomScrollbar.min.css">
    <link rel="stylesheet" href="css/style-main.css">
    <link rel="stylesheet" href="css/responsive.css">
    <style>

        tr > * {
            background-color: white!important;
        }
        .buttons:active {
            background-color: white!important;
        }
        .heighted {
            line-height: 2.4!important;
        }
        .table-rows tr td:first-child{
            border-top-left-radius:14px;
            border-bottom-left-radius:14px;
        }
        .table-rows tr td:last-child{
            border-top-right-radius:14px;
            border-bottom-right-radius:14px;
        }
        .table-rows tr {
            gap: 20px;
        }
        .table td, .table th {
            border-top: none!important;
        }
        .doc_banner_area {
            background: #537395;
        }

    </style>

    <title>Docy</title>

</head>

<body data-scroll-animation="true">
    <div id="preloader">
        <div id="ctn-preloader" class="ctn-preloader">
            <div class="round_spinner">
                <div class="spinner"></div>
                <div class="text">
                    <img src="img/spinner_logo.png" alt="">
                    <h4><span>Doc</span>y</h4>
                </div>
            </div>
            <h2 class="head">Did You Know?</h2>
            <p></p>
        </div>
    </div>
    <div class="body_wrapper">
        <nav class="navbar navbar-expand-lg menu_one menu_purple sticky-nav">
            <div class="container">
                <a class="navbar-brand header_logo" href="index.html">
                    <img class="first_logo sticky_logo" src="img/logo.png" srcset="img/logo-2x.png 2x" alt="logo">
                    <img class="white_logo main_logo" src="img/logo-w.png" srcset="img/logo-w2x.png 2x" alt="logo">
                </a>
                <button class="navbar-toggler collapsed" type="button" data-toggle="collapse"
                    data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                    aria-label="Toggle navigation">
                    <span class="menu_toggle">
                        <span class="hamburger">
                            <span></span>
                            <span></span>
                            <span></span>
                        </span>
                        <span class="hamburger-cross">
                            <span></span>
                            <span></span>
                        </span>
                    </span>
                </button>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav menu ml-auto">
                        <li class="nav-item dropdown submenu active">
                            <a href="/" class="nav-link dropdown-toggle">Home</a>
                            <i class="arrow_carrot-down_alt2 mobile_dropdown_icon" aria-hidden="false"
                                data-toggle="dropdown"></i>
                        </li>
                        
                    </ul>
                    <div class="right-nav">
                        <div class="px-2 js-darkmode-btn" title="Toggle dark mode">
                            <label for="something" class="tab-btn tab-btns">
                                <ion-icon name="moon"></ion-icon>
                            </label>
                            <label for="something" class="tab-btn">
                                <ion-icon name="sunny"></ion-icon>
                            </label>
                            <label class=" ball" for="something"></label>
                            <input type="checkbox" name="something" id="something" class="dark_mode_switcher">
                        </div>
                    </div>
                </div>
            </div>
        </nav>

        <section class="doc_banner_area banner_creative1">
            <ul class="list-unstyled banner_shap_img">
                <li><img src="img/new/banner_shap1.png" alt=""></li>
                <li><img src="img/new/banner_shap4.png" alt=""></li>
                <li><img src="img/new/banner_shap3.png" alt=""></li>
                <li><img src="img/new/banner_shap2.png" alt=""></li>
                <li><img data-parallax='{"x": -180, "y": 80, "rotateY":2000}' src="img/new/plus1.png" alt=""></li>
                <li><img data-parallax='{"x": -50, "y": -160, "rotateZ":200}' src="img/new/plus2.png" alt=""></li>
                <li></li>
                <li></li>
                <li></li>
            </ul>
            <form action="#" id="cform" method="POST" enctype="multipart/form-data">
                <div class="container">
                    <div class="doc_banner_content">
                        <h2 class="wow fadeInUp"> Choose your file </h2>
                        <p class="wow fadeInUp" data-wow-delay="0.2s"> Select a file to start conversion </p>
                        <form action="#" class="header_search_form">
                            <div class="header_search_form_info">
                                <div class="form-group">
                                    <div class="d-flex mb-2 justify-content-center">
                                        <div class="btn-group dropdown">
                                            <button type="button" class="btn btn-primary btn-lg triggerbutton">
                                                <i class="icon icon_plus mr-1"></i>
                                                Upload File
                                            </button> 
                                            <input type="file" multiple="multiple" name="images" class="d-none main-input" accept="image/*">
                                        </div>
                                        <button 
                                            type="button" 
                                            class="btn btn-primary btn-lg mx-2 dropdown-toggle" 
                                            data-toggle="dropdown" 
                                        >
                                            <i class="icon icon_link mr-1"></i>
                                            Upload By URL
                                        </button> 
                                        <div class="dropdown-menu dropdown-menu-right">
                                            <div class="container" style="width:500px;">
                                                <div class="d-flex" style="gap:5px">
                                                    <input 
                                                        type="text" 
                                                        name="url" 
                                                        class="form-control" 
                                                        placeholder="paste a URL here to upload"
                                                    >
                                                    <button class="btn btn-success uploader-btn" >Upload</button>
                                                </div>
                                            </div>
                                        </div> 
                                    </div>
                                </div>
                            </div>
                        </form>

                    </div>
                    <div class="doc_banner_content uploaded">
                        <table class="table table-striped table-hover">
                            <tbody class="table-rows">
                            </tbody>
                        </table>
                        <button class="position-absolute btn bg-white" style="left:10px">Add more</button>
                        <button type="submit" class="position-absolute btn bg-white convert" style="right:10px">Convert</button>
                    </div>
                    
                </div>
            </form>

        </section>
        
        <footer class="doc_footer_area">
            <div class="doc_footer_top bg-transparent">
                <div class="container">
                    <div class="row">
                        <div class="col-lg-4 col-sm-6">
                            <div class="f_widget doc_about_widget wow fadeInUp" data-wow-delay="0.2s">
                                <a href="#">
                                    <img src="img/logo.png" class="logo-light" srcset="img/logo-2x.png 2x" alt="">
                                    <img src="img/logo-w.png" class="logo-dark" srcset="img/logo-2x.png 2x" alt="">
                                </a>
                                <p>I’m available for commissions and collaborations, and i’m excited to hear from you
                                    about
                                    new projects.!!</p>
                                <ul class="list-unstyled">
                                    <li><a href="#"><i class="social_facebook"></i></a></li>
                                    <li><a href="#"><i class="social_twitter"></i></a></li>
                                    <li><a href="#"><i class="social_vimeo"></i></a></li>
                                    <li><a href="#"><i class="social_linkedin"></i></a></li>
                                </ul>
                            </div>
                        </div>
                        <div class="col-lg-2 col-sm-6">
                            <div class="f_widget doc_service_list_widget pl-30 wow fadeInUp" data-wow-delay="0.3s">
                                <h3 class="f_title_two">Solutions</h3>
                                <ul class="list-unstyled">
                                    <li><a href="#"><img src="img/new/smile2.png" alt="">Help Docs</a></li>
                                    <li><a href="#"><img src="img/new/doc2.png" alt="">Docbuzz</a></li>
                                    <li><a href="#"><img src="img/new/house2.png" alt="">User Frontend</a></li>
                                    <li><a href="#"><img src="img/new/bag2.png" alt="">Lightbox</a></li>
                                </ul>
                            </div>
                        </div>
                        <div class="col-lg-3 col-sm-6">
                            <div class="f_widget doc_service_list_widget pl-100 wow fadeInUp" data-wow-delay="0.4s">
                                <h3 class="f_title_two">Support</h3>
                                <ul class="list-unstyled">
                                    <li><a href="#">Help Desk</a></li>
                                    <li><a href="#">Knowledge Base</a></li>
                                    <li><a href="#">Live Chat</a></li>
                                    <li><a href="#">Integrations</a></li>
                                    <li><a href="#">Reports</a></li>
                                    <li><a href="#">Messages</a></li>
                                </ul>
                            </div>
                        </div>
                        <div class="col-lg-3 col-sm-6">
                            <div class="f_widget doc_service_list_widget pl-70 wow fadeInUp" data-wow-delay="0.5s">
                                <h3 class="f_title_two">Company</h3>
                                <ul class="list-unstyled">
                                    <li><a href="#">About Us</a></li>
                                    <li><a href="#">Testimonials</a></li>
                                    <li><a href="#">Affiliates</a></li>
                                    <li><a href="#">Partners</a></li>
                                    <li><a href="#">Careers</a></li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="doc_footer_bottom">
                <div class="container d-flex justify-content-between">
                    <ul class="doc_footer_menu list-unstyled wow fadeInUp" data-wow-delay="0.2s">
                        <li><a href="#">Blog</a></li>
                        <li><a href="#">Latest Projects</a></li>

                    </ul>
                    <p class="wow fadeInUp" data-wow-delay="0.3s">© 2023 All Rights Reserved Design by
                        <span>Spider-themes</span>
                    </p>
                </div>
            </div>
        </footer>
    </div>

    <!-- Back to top button -->
    <a id="back-to-top" title="Back to Top"></a>

    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="js/jquery-3.5.1.min.js"></script>
    <script src="assets/bootstrap/js/popper.min.js"></script>
    <script src="assets/bootstrap/js/bootstrap.min.js"></script>
    <script src="js/pre-loader.js"></script>
    <!-- <script src="assets/slick/slick.min.js"></script> -->
    <script src="js/jquery.parallax-scroll.js"></script>
    <script src="assets/niceselectpicker/jquery.nice-select.min.js"></script>
    <!-- <script src="assets/wow/wow.min.js"></script> -->
    <script src="assets/mcustomscrollbar/jquery.mCustomScrollbar.concat.min.js"></script>
    <script src="assets/magnify-pop/jquery.magnific-popup.min.js"></script>
    <script src="js/plugins.js"></script>
    <script src="https://unpkg.com/ionicons@5.4.0/dist/ionicons.js"></script>
    <script src="js/main.js"></script>

    <script>
        
        const wrapText = (text, maxLength) => {
            if (text?.length > maxLength) {
                let truncatedText = text.substring(0, maxLength) + '...';
                return truncatedText;
            }
            return text
        }

        let images = [];

        $(function(){

            $('.triggerbutton').click(function(){
                $('.main-input').trigger('click')
            })

            $('.main-input').on('change', function(e){
                
                $('.doc_banner_content .table-rows').children().remove();
                let files = e.target.files;
                [...files].forEach( (file,i) => {
                    images.push(file)
                });
                makePreview()

            });

        })

        let i = 0;

        function makePreview() {

            let length = $('.doc_banner_content .table-rows tr')?.length + 1;
            
            $('.doc_banner_content .table-rows').children().remove();

            for (let index = 0; index < images.length; index++) {
                const file = images[index];
                var reader = new FileReader();
                reader.readAsDataURL(file);
                reader.onload = function() {
                    
                    $('.doc_banner_content .table-rows').append(`<tr class="mt-2 w-100" data-index="${index}">\
                        <td style="width:35%">
                            <b class="fs-14 mb-1 heighted">${wrapText(file.name, 40)}</b>\
                            <input type="hidden" name="names=[]" value="${file.name}">
                        </td>
                        <td style="width:25%">
                            <div style="display:flex" class="dropdown">
                                <div class="avatar-sm bg-light heighted">\
                                    <span>Convert To</span>
                                </div>\
                                <button data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" class="mx-3 btn dropdown-toggle dropdown-toggle-split">
                                    <span data-name="${file.name}">...</span>
                                    <ul class="dropdown-menu" id="navbarSupportedContent">
                                        <li class="dropdown-item buttons">
                                            <div class="row">
                                                <div class="card" data-index="${index}">
                                                    <a class="btn text-white bg-dark" >JPEG</a>    
                                                </div>
                                                <div class="card" data-index="${index}">
                                                    <a class="btn text-white bg-dark" >PNG</a>    
                                                </div>
                                                <div class="card" data-index="${index}">
                                                    <a class="btn text-white bg-dark" >WEBP</a>    
                                                </div>
                                                <div class="card" data-index="${index}">
                                                    <a class="btn text-white bg-dark" >AVIF</a>    
                                                </div>
                                                <div class="card" data-index="${index}">
                                                    <a class="btn text-white bg-dark" >SVG</a>    
                                                </div>
                                                <div class="card" data-index="${index}">
                                                    <a class="btn text-white bg-dark" >ICO</a>    
                                                </div>
                                            </div>
                                        </li>
                                    </ul>
                            </div>
                        </td>
                        <td style="width:25%" class="action-${index}"></td>
                        <td style="width:15%">
                            <i class="icon_close text-danger" onclick="removeSpecificFile(${index})" style="font-size:1.8rem;cursor:pointer"></i>
                        </td>
                    </tr>`);

                    $('.buttons .card').on('click', function(e){
                        $('tr[data-index="'+ this.dataset.index +'"] td .dropdown-toggle span').text(this.innerText);
                    })
                    $('tfoot tr').css('display', $('tbody tr').length? 'flex':'none');

                }

                
            }
           

        }

        function removeSpecificFile(index) {
            // return console.log(index)
            const input = document.querySelector('input[type=file].d-none')
            images.splice(index,1)
            // delete images[index];
            const newFileList = Object.values(images)
            let dataTransfer = new DataTransfer();
            input.files=null
            input.value='';
            $('.doc_banner_content .table-rows').children().remove();
            newFileList.forEach(file => {
                if(typeof file==='object'){
                    dataTransfer.items.add(file)
                    var reader = new FileReader();
                    reader.readAsDataURL(file);
                    reader.onload = function() {
                        makePreview(reader.result, file.name, newFileList.indexOf(file));
                    }
                }
            });

            if(newFileList[0] && typeof newFileList[0]==='object'){
                input.files = dataTransfer.files;
            }

            $('tr[data-index="'+index+'"]').remove();
            event.preventDefault();

        }

        $('#cform').submit(function(e){
            e.preventDefault();
            const fd = new FormData();//$('#cform')[0]
            const meta = {};
            $('td .dropdown-toggle span').each(function(k,span) {
                meta[span.dataset.name]=span.innerText;
            });

            const route= document.querySelector('input[name="images"]').files.length > 1? "convert-multiple":"convert";
            fd.append('_meta', JSON.stringify(meta));
            // fd.append('images', images)
            images.forEach(f=> fd.append('images',f ));

            const extension = Object.values(meta)[0].toLowerCase()
            const filename = Object.keys(meta)[0].split(".")[0]

            $.ajax({
                url: "http://localhost:3100/" + route,
                type:"POST",
                processData:false,
                contentType:false,
                data:fd,
                xhrFields: {
                    responseType: 'blob' // 👈 important!
                },
                success:function (blob) {
                    const url = window.URL.createObjectURL(blob);
                    const a = document.createElement('a');
                    a.href = url;
                    if(route === 'convert-multiple') {
                        a.download = 'converted.zip';
                    } else {
                        a.download = filename+'-converted.'+extension;
                    }
                    document.body.appendChild(a);
                    a.click();
                    a.remove();
                },
                error: err=> console.log(err)
            });
        })

        const imageURLs = [];

        $('.uploader-btn').click( async function(e) {

            e.preventDefault();
            e.stopImmediatePropagation();
            e.stopPropagation();
            const url = $('input[name=url]').val();

            // https://upload.wikimedia.org/wikipedia/en/2/21/Web_of_Spider-Man_Vol_1_129-1.png
            let filename = url.split('/');
            filename = filename[filename.length-1];
            
            let blob;
            try {

                const res = await fetch(url);
                if (!res.ok) {
                    throw new Error(`HTTP error! Status: ${res.status}`);
                }
                blob = await res.blob();

            } catch (err) {
                console.error('Fetch or blob conversion failed:', err.message);
                // Handle error — show message, retry, fallback, etc.
                return alert("Something wrong with the url."+err.message)
            }

            const file = new File([blob], filename, {
                type: blob.type,
                lastModified: Date.now(),
            });
            imageURLs.push(file);
            images.push(file);

            console.log(Object.values(images))
            const newFileList = Object.values(images)
            let dataTransfer = new DataTransfer();
            const input = document.querySelector('input[name="images"]');
            input.files=null
            input.value='';
            if(newFileList[0] && typeof newFileList[0]==='object'){
                console.log('vyawastha to hai..', dataTransfer.files)
                input.files = dataTransfer.files;
            }

            makePreview()
        });

    </script>
</body>

</html>